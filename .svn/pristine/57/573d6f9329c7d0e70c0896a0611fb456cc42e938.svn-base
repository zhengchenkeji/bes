package com.zc.connect.analysis.impl;

import com.zc.connect.analysis.AnalysisStrategy;
import com.zc.connect.analysis.AnalysisTypeEnum;
import org.springframework.stereotype.Service;

import java.util.Objects;

/**
 * @author Athena-YangChao
 * @description
 * @date 2021/12/8/008
 * @apiNote
 */
@Service
public class AnalysisStrategy16toFloat implements AnalysisStrategy {
    @Override
    public String type() {
        return AnalysisTypeEnum.AnalysisStrategy16toFloat.getCode();
    }

    @Override
    public boolean isTypeMatch(String type) {
        return Objects.equals(type, type());
    }

    @Override
    public void analysisStrategy(String type) {
        System.err.println("走：==》AnalysisStrategy16toFloat");
    }
}
